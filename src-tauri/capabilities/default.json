{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "register-paper", "edit-paper-*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
